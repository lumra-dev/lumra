# @lumra/config 

This repository is used for storing the basic .tsconfig settings, along with the schema and JSONC parsing for the filesystem router's structure.

`packages/config/src/_lumra` is the routing structure of the Luma configuration file.